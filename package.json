{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "cross-env process.env.NODE_ENV=test jest --config=jest.config.js",
    "test-watch": "npm test -- --watch"
  },
  "engines": {
    "node": "9.1.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^5.1.1",
    "express": "^4.16.2",
    "socket.io": "^2.0.4"
  },
  "devDependencies": {
    "jest": "^21.2.1"
  }
}
